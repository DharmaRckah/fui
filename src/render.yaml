services:
  - name: your-react-app
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start # If you have a server-side component
    envVars:
      NODE_ENV: production
    staticPublishPath: dist # Path to your built static files
